#include <stdio.h>
// #include <memory>
#include "MathVector2.hpp"

template<typename T>
constexpr decltype(auto) test(ink::reference_to<T> auto&& from) { return from; }

int main([[maybe_unused]] int argc, [[maybe_unused]] const char* argv[]) {
	
	
	
	return 0;
}
